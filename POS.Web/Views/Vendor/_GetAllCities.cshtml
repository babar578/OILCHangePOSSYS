@using POS.Utilities.ViewModel
@model IEnumerable<CityViewModel>
@{
    Layout = null;
}

<style>
    .cities-container {
        padding: 20px;
    }
    .empty-cities {
        text-align: center;
        padding: 60px 20px;
        color: #718096;
    }
    .empty-cities i {
        font-size: 64px;
        color: #cbd5e0;
        margin-bottom: 20px;
    }
</style>

<script src="~/Scripts/Site.js"></script>
<div class="cities-container">
    <div class="row">
        <div class="col-12">
            <div class="modern-card">
                <div class="modern-card-header">
                    <h3><i class="fas fa-map-marker-alt"></i> Cities Management</h3>
                    <a href="/Vendor/City" id="btn-add"
                       class="btn-modern-primary"
                       data-toggle="tooltip" data-placement="bottom" title="Add New City"
                       data-caption="Add New City">
                        <i class="fas fa-plus"></i> Add New City
                    </a>
                </div>
                <div class="modern-table-wrapper">
                    @if (Model != null && Model.Count() > 0)
                    {
                        <table class="modern-table">
                            <thead>
                                <tr>
                                    <th>Code</th>
                                    <th>City Name</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var e in Model)
                                {
                                    <tr class="dgrow" data-id="@e.Id">
                                        <td>@e.Id</td>
                                        <td><strong>@e.Name</strong></td>
                                        <td>
                                            <div class="action-buttons">
                                                <a href="/Vendor/City/@e.Id" class="action-btn-edit" data-toggle="tooltip" data-placement="bottom" title="Edit City">
                                                    <i class="fas fa-edit"></i> Edit
                                                </a>
                                                <a href="/Vendor/DeleteCity/@e.Id" class="action-btn-delete" data-toggle="tooltip" data-placement="bottom" title="Delete City">
                                                    <i class="fas fa-trash"></i> Delete
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <div class="empty-cities">
                            <i class="fas fa-map-marker-alt"></i>
                            <h4>No Cities Found</h4>
                            <p>Get started by adding your first city.</p>
                            <a href="/Vendor/City" class="btn-modern-primary" style="margin-top: 20px;">
                                <i class="fas fa-plus"></i> Add City
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
