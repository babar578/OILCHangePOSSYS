@using POS.Utilities.ViewModel
@model IEnumerable<CategoryViewModel>
@{
    Layout = null;
}

<style>
    .raw-categories-container {
        padding: 20px;
    }
    .empty-categories {
        text-align: center;
        padding: 60px 20px;
        color: #718096;
    }
    .empty-categories i {
        font-size: 64px;
        color: #cbd5e0;
        margin-bottom: 20px;
    }
</style>

<script src="~/Scripts/Site.js"></script>
<div class="raw-categories-container">
    <div class="row">
        <div class="col-12">
            <div class="modern-card">
                <div class="modern-card-header">
                    <h3><i class="fas fa-tags"></i> Raw Categories Management</h3>
                    <a href="/Vendor/RawCategory" id="btn-add"
                       class="btn-modern-primary"
                       data-toggle="tooltip" data-placement="bottom" title="Add New Raw Category"
                       data-caption="Add New Raw Category">
                        <i class="fas fa-plus"></i> Add New Raw Category
                    </a>
                </div>
                <div class="modern-table-wrapper">
                    @if (Model != null && Model.Count() > 0)
                    {
                        <table class="modern-table">
                            <thead>
                                <tr>
                                    <th>Code</th>
                                    <th>Category Name</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var e in Model)
                                {
                                    <tr class="dgrow" data-id="@e.Id">
                                        <td>@e.Id</td>
                                        <td><strong>@e.Name</strong></td>
                                        <td>
                                            <div class="action-buttons">
                                                <a href="/Vendor/RawCategory/@e.Id" class="action-btn-edit" data-toggle="tooltip" data-placement="bottom" title="Edit Raw Category">
                                                    <i class="fas fa-edit"></i> Edit
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <div class="empty-categories">
                            <i class="fas fa-tags"></i>
                            <h4>No Raw Categories Found</h4>
                            <p>Get started by adding your first raw category.</p>
                            <a href="/Vendor/RawCategory" class="btn-modern-primary" style="margin-top: 20px;">
                                <i class="fas fa-plus"></i> Add Raw Category
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
