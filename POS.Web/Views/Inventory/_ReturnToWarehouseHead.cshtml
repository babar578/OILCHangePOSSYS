@using POS.Utilities.Services
@using POS.Utilities.ViewModel
@model ReturnToWarehouseHeadViewModel
@{

    Layout = null;
    Model.Locations = VendorServices.GetAllLocations();
    Model.Departments = ItemServices.GetAllDepartments();
    Model.IssueToLocationDetails = VendorServices.GetAllIssueToLocationItemsName();
}

@using (Html.BeginForm("", "", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.Id)
    
    <div class="card card-custom">
        <div class="card-header-custom">
            <i class="fas fa-warehouse"></i>
            <span>Return To Warehouse</span>
        </div>
        <div class="card-body" style="padding: 20px;">
            <div class="row">
                <div class="col-md-3">
                    <div class="form-group">
                        <label class="col-form-label">Transaction Date <span class="text-danger">*</span></label>
                        <div class="input-group">
                            <div class="input-group-prepend">
                                <span class="input-group-text"><i class="far fa-calendar-alt"></i></span>
                            </div>
                            @Html.TextBoxFor(m => m.TransactionDate, new { @type = "date", @class = "form-control", placeholder = "Transaction Date * ", @required = "true" })
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="form-group">
                        <label class="col-form-label">Doc No</label>
                        <div class="input-group">
                            <div class="input-group-prepend">
                                <span class="input-group-text"><i class="fas fa-file-alt"></i></span>
                            </div>
                            @Html.TextBoxFor(m => m.DocNo, new { @class = "form-control", placeholder = "DocNo ", @required = "true" })
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="form-group">
                        <label class="col-form-label">Location <span class="text-danger">*</span></label>
                        @Html.DropDownListFor(m => m.LocationId, new SelectList(Model.Locations, "Id", "Name"), new { @class = "form-control selectpicker", data_live_search = "true", data_hide_disabled = "true", title = "Select Location", @required = "true" })
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="form-group">
                        <label class="col-form-label">Department <span class="text-danger">*</span></label>
                        @Html.DropDownListFor(m => m.DepartmentId, new SelectList(Model.Departments, "Id", "Name"), new { @class = "form-control selectpicker", data_live_search = "true", data_hide_disabled = "true", title = "Select Department", @required = "true" })
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="col-form-label">Remarks</label>
                        @Html.TextBoxFor(m => m.Remarks, new { @class = "form-control", placeholder = "Enter Remarks", @required = "true" })
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="col-form-label">Add Item</label>
                        <div class="input-group">
                            @Html.DropDownListFor(m => m.ItemId, new SelectList(Model.IssueToLocationDetails, "ItemId", "ItemName"), new { @class = "form-control selectpicker", data_live_search = "true", data_hide_disabled = "true", title = "Select Item to Return", @required = "true" })
                            <div class="input-group-append">
                                <button type="button" id="btnImport" class="btn btn-primary"><i class="fas fa-plus"></i> Import</button>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="form-group">
                        <label class="col-form-label">&nbsp;</label>
                        <button type="button" onclick="ReturnToWarehouse()" class="btn btn-block btn-primary btn-action shadow-sm"><i class="fas fa-save"></i> SAVE</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

<script>
    $(document).ready(function () {
        $('select').selectpicker();
    });
    var today = new Date();
    var min = today.getMonth() + 1;
    var dd = today.getDate();
    var mm = today.getMonth() + 1; //January is 0!
    var yyyy = today.getFullYear();
    if (dd < 10) {
        dd = '0' + dd
    }
    if (mm < 10) {
        mm = '0' + mm
    }

    today = yyyy + '-' + mm + '-' + dd;
    document.getElementById("TransactionDate").setAttribute("max", today);
    min = yyyy + '-' + mm + '-' + dd;
    document.getElementById("TransactionDate").setAttribute("min", min);

    $("#btnImport").click(function () {
        var itemId = $("#ItemId").val();
        startLoader();
        $.ajax(
            {
                url: "/Inventory/SearchReturnToWarehouseItem?ItemId=" + itemId,
                type: "POST"
            }
        ).done(function (result) {
            ReturnToWarehouseDetail();
            stopLoader();
        });
    });

</script>






